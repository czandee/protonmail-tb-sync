{
  "name": "protonmail-labels-to-tags",
  "version": "0.1.0",
  "description": "Synchronizes ProtonMail labels (via Bridge) to Thunderbird tags",
  "private": true,
  "license": "GPL-3.0-only",
  "scripts": {
    "lint": "web-ext lint",
    "build": "web-ext build --overwrite-dest",
    "start": "web-ext run -t thunderbird"
  },
  "devDependencies": {
    "web-ext": "^10.6.0"
  }
}
